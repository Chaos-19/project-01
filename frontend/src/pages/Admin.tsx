import React from "react";
import {
    AddProductForm,
    Notification,
    ProductList
} from "../components/admin/index";

interface Props {}

const Admin = (/*props: Props*/) => {
    return (
        <div>
            <ProductList />
        </div>
    );
};

export default Admin;
